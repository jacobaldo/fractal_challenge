import {StyleSheet} from 'react-native';
import {spacings} from '../../constants/spacings';

export default StyleSheet.create({
  noNextPage: {paddingVertical: spacings['2xl'], alignItems: 'center'},
});
